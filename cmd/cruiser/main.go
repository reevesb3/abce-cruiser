// Command cruiser is the AB Driver's Ed slot grabber CLI. All logic lives in
// the internal/app package; this is just the entry point.
package main

import "cruiser/internal/app"

func main() { app.Main() }
